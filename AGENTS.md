# 项目说明（AGENTS.md）

> 本文件供 AI agent 阅读，记录项目的关键事实与约定。

## 协作约定

- **本项目为个人独立开发**，无团队协作、无 MR/PR 流程。
- **Git 工作流**：如无特殊说明，**可直接在 `main` 分支上修改并提交**（commit + push），无需另开 `feature/*` / `fix/*` 分支，也无需走受保护分支的合并流程。
  - 全局 AGENTS.md 中「禁止在受保护分支直接提交」的规则，**在本项目不适用**，以本节为准。
  - 例外：当用户明确要求走分支或提 PR 时，再按分支流程操作。
- **提交信息**：使用中文，简要说明本次改动内容。

## 数据来源

本项目所有游戏数据均来源于 **[http://guagame.com](http://guagame.com/)**（挂机传奇，GeeM2 1.76 服务端）。

- **数据获取方式**：从 guagame.com 的游戏源码中提取（站点已迁移目录：引擎 JS 在 `/js/`，数据在 `/data/`，入口 `/index.html`）
- **数据时效**：以抓取时服务端版本为准，若服务端更新需重新核对
- **最近核查**：2026-08-17 重新抓取全量比对，服务端数值与本地完全一致（零差异）；原版参考源码见 `docs/reference/`（origin-game.js / origin-ui.js / origin-api.js）
- **数据文件**（位于项目根目录）：

| 文件 | 内容 | 数据量 |
|------|------|--------|
| `expTable.js` | 经验表 | 100 级 + |
| `jobStats.js` | 职业成长（战/法/道，260 级） | 3 职业 |
| `monsters.js` | 怪物（HP/攻防/经验/技能） | 281 只 |
| `maps.js` | 地图（怪物投放/等级要求） | 122 张 |
| `items.js` | 装备物品（12 类） | 348 件 |
| `skills.js` | 技能（6 类） | 60 个 |
| `drops.js` | 掉落表（专属 + 通用） | 96 怪专属 + 3 档通用 |
| `tower.js` | 通天塔副本（30 层怪物/门票） | 30 层 |

> ⚠️ 修改任何游戏数据时，请以 guagame.com 源码为唯一事实来源，不要凭经验/猜测编造数值。
